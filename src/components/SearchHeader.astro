<header id="header">
  <div class="container searchbar">

    
        <a href="javascript:history.back()" class="btn-back" aria-label="boton atras">
            <svg xmlns="http://www.w3.org/2000/svg" width="40" height="20" viewBox="0 0 14 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="19" y1="12" x2="5" y2="12"></line><polyline points="12 19 5 12 12 5"></polyline></svg>
        </a>

        
        <input 
            type="text" 
            id="main-search" 
            placeholder="Buscar por nombre o característica..." 
            autocomplete="off"
        />
  </div>
</header>

<style>
    #header{
		background:linear-gradient(180deg, var(--darkh) 50%, rgba(0,0,0,.05) 100%);
		backdrop-filter: blur(10px);
	}
    
  .btn-back svg{
    margin-top: .5rem;
    width:70px;
    height:30px;
  }
  .searchbar{
        height:var(--hheader);
    display:flex;
    flex-flow:row nowrap;
    gap:1rem;
    padding: 1rem;
  }
 

  input {
    width: 100%;
    padding: 0.9rem 1.2rem;
    border-radius: 8px;
    border: 1px solid #333;
    background: #111;
    color: #fff;
    font-size: 1rem;
    transition: all 0.2s;
  }

  .search-input-wrapper input:focus { border-color: #f39c12; outline: none; background: #000; }
</style>