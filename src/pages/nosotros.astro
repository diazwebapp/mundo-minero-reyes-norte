---
import Layout from '../layouts/Layout.astro'
import About from '../components/About.astro';
import { BASE_URL, getPermalink } from '../utils/config';

const canonical = getPermalink(["nosotros"])

---

<Layout title="La empresa" description="Maquinaria y equipos para minería. MUNDO MINERO LOS REYES DEL NORTE, Lideres en equipos de extracción minera." canonical={canonical} schemas={[]}>
  <main>
    <About/>
  </main>
</Layout>